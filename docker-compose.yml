services:
  claude:
    build:
      context: .
      args:
        USER_UID: ${USER_UID:-501}
    volumes:
      - ~/.claude:/home/claude/.claude
      - .:/app
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
    stdin_open: true
    tty: true
